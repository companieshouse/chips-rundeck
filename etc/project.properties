# project.properties

#
# The base directory for this project's instances
#
project.dir = /apps/rundeck/projects/${project.name}
#
# The base directory of project specific configuration files
#
project.etc.dir = /apps/rundeck/projects/${project.name}/etc

#
# The default resources file
#
resources.source.1.type=file
resources.source.1.config.file=/apps/rundeck/projects/${project.name}/etc/resources.xml
resources.source.1.config.format=resourcexml
resources.source.1.config.includeServerNode=true
resources.source.1.config.generateFileAutomatically=true
